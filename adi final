 const formattedData = response.data.result.map((item, index) => ({
            ...item,
            index: index + 1,
            daysOfLeave: item.daysOfLeave + (item.hoursOfLeave / 8), // Convert hours to days
        }));

        setTableData(formattedData);
