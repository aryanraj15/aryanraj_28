 const handleButtonClickPwd = () => {

        fileInputRefPwd.current.click();
    }

 <VisuallyHiddenInput

                                ref={fileInputRef3}
                                type="file"
                                onChange={handleFileChange3}
                            />


                            {panUploadedFile && (
                                <Typography>Uploaded File: {panUploadedFile.name}</Typography>
                            )}
