const handleFileChange3 = (event) => {

        const File = event.target.files[0];
        console.log(File.name)
        //  setSelectedFile(File)

        selectedFile = event.target.files[0];
        // setAttachmentId(98)
        attachmentId = 98
        setPanUploadedFile(selectedFile)
        if (selectedFile) {
            uploadAttachment()
        }
    }

 <Button sx={{ mb: 3 }} component="label" variant="contained" startIcon={<CloudUploadIcon />} onClick={handleButtonClick3} >
                                Upload PAN Card
                            </Button>
                            {/* <VisuallyHiddenInput type="file" /> */}
                            <VisuallyHiddenInput

                                ref={fileInputRef3}
                                type="file"
                                onChange={handleFileChange3}
                            />
