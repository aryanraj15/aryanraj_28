     for (let i = indexToDelete; i <= updatedRows.length; i++) {
                const rowToUpdate = updatedRows.find((row) => row.index === i);
                if (rowToUpdate) {
                    rowToUpdate.index = i;
